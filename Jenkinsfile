pipeline
{
    agent {
      node { label 'EC2' }
           }
    stages {
        stage('Hello') {
            steps {
                sh 'mvn --version'
                   }
                        }
    }
}
